# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Godot Web 导出产物用 Git LFS 跟踪（pck 近200MB，wasm 38MB）
build/web/**/*.pck filter=lfs diff=lfs merge=lfs -text
build/web/**/*.wasm filter=lfs diff=lfs merge=lfs -text
build/web/**/*.js filter=lfs diff=lfs merge=lfs -text
